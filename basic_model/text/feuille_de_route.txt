To do:

- Créer le setup pour les nouvelles données (les plus grosses): dans le run créer un argument selon le type de données indiquant si on veut li_modes ou juste index_modes (important: un saveRDS serait trompeur!!). Dans le cas FALSE, inputer volontairement une liste vide/ ou alors changer la façon dont ça se décide en multibloc (mieux...)

-Comparer sur des petits échantillons les perfs des algos simple et multibloc sur 40 runs pour évacuer les doutes. On veut des seuils d'indécision sous les 1%. On prendra une nouvelle seed pour confirmer les résultats. Reste le logistique simple qui bug en "explosant" quand en parallèle

- Changer le plot pour faire par mode et par bloc à chaque fois (mais sans faire les associations bloc_mode inexistantes). Checker où ça en est...



Encadrants:

- Ruche 
- Pyradio comprendre l'extraction et refaire pour la shape.
- Comparer à une zone saine à chaque fois pour le réhaussement.
- Renormaliser les pénalisations en fontion du bloc/ nombre de variable: un lambda par bloc.

Erreurs:

Warnings:

-Dans les plots : No shared levels found between `names(values)` of the manual scale and the data's colour values



infos:

- En multibloc, dans li_index_modes et li_name_modes, ne pas mettre les index_slice et names relatifs à la modalité: variable. logistic_multibloc.r gérer cela tout seul. Il faut aussi bien nommer chaque mode dans les deux




