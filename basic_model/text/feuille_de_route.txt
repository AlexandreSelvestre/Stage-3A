To do:

- Reprendre rsync -P -r ./data selvestra@ruche.mesocentre.universite-paris-saclay.fr:/workdir/selvestra

-Comparer sur des petits échantillons les perfs des algos simple et multibloc sur 40 runs pour évacuer les doutes (reste à faire simple). On veut des seuils d'indécision sous les 1%. On prendra une nouvelle seed pour confirmer les résultats. Reste le logistique simple qui bug en "explosant" quand en parallèle

-Pertinence du double cercle? (C'est assez nul tout compte fait...: ne se justifie pas!)

-Améliorer l'extraction des sains sur les tumeurs vides / celles où la bordure extérieure est favorisée à tort

- Garder des NA dans le df des slices dont la partie saine est vide (pour signaler que c'est vide!!)

-Comprendre le miracle de la fonction sd dans le dataframe brute_data de multislice (qui sait gérer des colonnes avec des characters????/ nombres sous forme de character????)


Erreurs:

Warnings:

-Dans les plots : No shared levels found between `names(values)` of the manual scale and the data's colour values



infos:

- En multibloc, dans li_index_modes et li_name_modes, ne pas mettre les index_slice et names relatifs à la modalité: variable. logistic_multibloc.r gérer cela tout seul. Il faut aussi bien nommer chaque mode dans les deux




