To do:

-Comparer sur des petits échantillons les perfs des algos simple et multibloc sur 40 runs pour évacuer les doutes. On veut des seuils d'indécision sous les 1%. On prendra une nouvelle seed pour confirmer les résultats. 

-Transformer le multibloc pour permettre d'analyser avec davantage de dimensions. 

Erreurs:

Warnings:

-Pour l'instant on supprime la normalisation selon les modes différent de la variable. On le rajoutera si nécessaire! 

-Problème si un mode s'appelle déjà "variable": corriger cela à la fin...

-Dans les plots : No shared levels found between `names(values)` of the manual scale and the data's colour values

-Checker le temps pris par le réordonnancement. Si c'est beaucoup, créer une table de rappel



